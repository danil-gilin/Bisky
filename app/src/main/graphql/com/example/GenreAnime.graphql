query GenreAnimeQuery($animeQuery: GeneralAnimeQuery!) {
    getAnimes(animeQuery: $animeQuery) {
        _id
        poster
        labels {
            en
            ru
        }
        score {
            averageScore
        }
        status
    }
}
